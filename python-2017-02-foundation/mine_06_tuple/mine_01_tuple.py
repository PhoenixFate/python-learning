# tuple定义完之后 无法进行修改
# 元组就是不能修改的数组
info_tuple = ('aa', 'bb', 'cc')

print(type(info_tuple))

# 定义只包含一个元素的元组
single_tuple = (5,)
print(type(single_tuple))

# 这样是int变量
# single_temp = (5)
# print(type(single_temp))
